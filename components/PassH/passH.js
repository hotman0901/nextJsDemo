import React from 'react';
import Link from 'next/link';

export const PassH = props => {
    return (
        <Link href={props.href}>
            <a>gogog</a>
        </Link>
    );
};

export default PassH;
